class Zoo
    def initialize(street, city, state, zip_code, address)
        @street = street
        @city = city
        @state = state
        @zip_code = zip_code
        @address = address
    end
end